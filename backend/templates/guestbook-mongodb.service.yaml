apiVersion: v1
kind: Service
metadata:
  name: go-leadopt-mongodb
  labels:
    app: leadopt
    tier: db
spec:
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: leadopt
    tier: db
